const log = value => console.log(`LOG: ${value}`);

const info = value => console.log(`INFO: ${value}`);

const debug = value => console.log(`DEBUG: ${value}`);

module.exports = {
  log,
  info,
  debug,
};
