import { info, debug } from '../../src/util/logger';

// Fucking no idea now...

/**
 * @param {string} s
 * @return {string}
 */
var longestPalindrome = function(s) {
    return s;
};

module.exports = {
  longestPalindrome;
}
