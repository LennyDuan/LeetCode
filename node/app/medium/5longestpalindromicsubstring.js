import { info, debug } from '../../src/util/logger';

/**
 * @param {string} s
 * @return {string}
 */
var longestPalindrome = function(s) {
    return s;
};

module.exports = {
  longestPalindrome;
}
